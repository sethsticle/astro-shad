# Graduation queue

The build→docs handoff ledger (see `frontend-contract.md` §8). Build sessions append rows with status `built`; docs sessions review each pending item against the contract, write its docs, and flip the row to `documented` with the date. Keep notes dense — they're the docs writer's briefing. If pending rows exceed ~10, run a docs session before building more.

Batch-wide note for the sections rows: hero-01/02/03 and cta-01 ship Unsplash demo images + token scrims as `background` slot fallbacks (`/photos/<id>/download?w=…` redirect URLs); docs should tell consumers to swap for local assets.

| Item | Built | Source / inspiration | Notes for the docs writer | Status |
|---|---|---|---|---|
| top-bar | 2026-07-17 | kca-astro sweep | Was docced under duplicate item `header01`; consolidated 2026-07-17 — duplicate registry item deleted, docs page/demos/nav renamed to top-bar. | documented |
| floating-header | 2026-07-17 | kca-astro sweep | Was docced under duplicate item `header02`; consolidated 2026-07-17 — duplicate deleted, docs renamed. DocsLayout itself uses it ("registry's first consumer"). | documented |
| accordion `highlight` prop | 2026-07-17 | user-added, ported to 02/03 | Docs refresh, not a new page: accordion page's props/demos don't mention `highlight` (accent border + text on the open item, all three variants). Demos `Single.astro`/`Boxed02.astro`/`Icons.astro` already use it. | documented 2026-07-17 |
| hero-01 | 2026-07-17 | layout sections plan (contract §7) | First section item; establish the docs "Layouts" nav group with this batch. SHIPS POPULATED (§7 reworked same day): `<Hero01 />` is the whole demo; customise = edit your installed copy (content block delimited by a `<!-- content — yours to edit -->` comment). Docs page should teach that model, not props. Imports Badge+Button from `../`; deps declared. `background` slot for the effects pass. | documented 2026-07-17 |
| hero-02 | 2026-07-17 | layout sections plan | Ships populated; content anchored bottom-left, editorial composition — docs should show a full-bleed image in the `background` slot. `min-h-svh` not `h-screen` (URL bars). Deps: button. | documented 2026-07-17 |
| hero-03 | 2026-07-17 | layout sections plan | Ships populated; centered hero + bottom card strip as auto-fit grid (`minmax(13rem,1fr)`) — 3 or 4 cells adapt, no prop. Deps: button, card. | documented 2026-07-17 |
| hero-04 | 2026-07-17 | shadcn-studio restaurant hero (simplified) | Split hero: headline left (rounded-full buttons, inline arrow svg in Button's icon-right slot), media placeholder right (stands in for the source's embla triple-synced carousel — rebuild planned as a carousel primitive, then a design pass on this section), icon-feature card row beneath (plain divs + inline lucide svgs, no card dep). Deps: button. | documented 2026-07-17 |
| split-01 | 2026-07-17 | layout sections plan | Ships populated; media half is a token-styled placeholder surface the consumer swaps for img/video. `reverse` prop = `md:order-last` (source order unchanged, mobile always stacks media-first; worth stating in docs). Deps: button. | documented 2026-07-17 |
| feature-grid-01 | 2026-07-17 | layout sections plan | Ships populated with three demo Cards; `columns` 3\|4 prop (add a fourth card for 4). Deps: card. | documented 2026-07-17 |
| cta-01 | 2026-07-17 | layout sections plan | Ships populated; narrow band on `bg-card` surface; `background` slot is INSIDE the band (gradient/glow later, effects pass). Deps: button. | documented 2026-07-17 |
| feature-section-01 | 2026-07-17 | shadcn-studio features section | Header block + six icon cards (2/3 cols). Demo content lives in a frontmatter FEATURES array (title/description/inline-lucide-path strings via set:html) — docs should teach editing the array. Per-card color props from the source became one neutral hover (`hover:border-primary/40`). Zero JS. Deps: button. | documented 2026-07-17 |
| feature-section-02 | 2026-07-17 | shadcn-space features section (motion/react) | Header row + testimonial-over-image panel (Unsplash eggs e6W48UPKijo — swap for local) + 2x2 muted icon cards. motion entrance anims → CSS transitions off `data-inview` (IntersectionObserver, SplitText pattern, source ease preserved, 100ms stagger via `--fs2-d`, reduced-motion safe). No avatar primitive yet — initials circle stands in. Deps: badge, button. | documented 2026-07-17 |
| logos-carousel | 2026-07-17 | user-supplied react LogosCarousel | Marquee ALTERNATIVE to logo-loop (docs should cross-link both). Markup contract: each DIRECT CHILD = one logo group (source's `count` chunking prop dropped — the consumer groups in markup). Rung-3 JS: interval flips `data-state` enter/exit per group; static `lc-enter`/`lc-exit` keyframes (translateY 40px + blur, fill both) via `is:global` style; per-logo stagger set as `--lc-d` from DOM index. Props: interval(ms)/stagger(s — mixed units mirror the source)/duration/initialDelay/gap. First group is the no-JS + reduced-motion + single-group fallback. No max-width baked in — consumer sets via class. | documented 2026-07-17 |
| highlighted-text | 2026-07-17 | user-supplied motion/react HighlightedText | Highlighter-swipe mark behind text; `mix-blend-difference` + LITERAL `text-white` inverts text as the mark passes — the literal is contract-sanctioned (blend math needs pure white both themes, commented inline). Mark is `bg-foreground` (≡ source's bg-black dark:bg-white). motion spring(30/300) ≈ `cubic-bezier(0.22,1,0.36,1)` 500ms transition off `data-inview`. Default plays on load (double-rAF so the transition runs); `inView` uses the SplitText IO pattern; `once={false}` replays on re-entry. No-JS = unhighlighted text; reduced-motion = static highlight. Props: from(4 edges)/delay/inView/once. | documented 2026-07-17 |
| button-group | 2026-07-17 | shadcn base-ui button-group | Three files: ButtonGroup + ButtonGroupText + ButtonGroupSeparator. cva → BASE + ORIENTATIONS maps; data-slot child selectors → plain `*:first/last-child` (works on any children); radius overrides carry `!` (would tie specificity with the child's own rounded-md otherwise). Separator INLINED (bg-input line, no separator primitive; orientation = the line's own axis, default vertical). ButtonGroupText: useRender → `as` prop. Source's select-trigger/input special-casing dropped. `data-slot="button-group"` kept for the nested-group gap-2 (split-button) selector. Zero JS. | documented 2026-07-17 |
| rotating-text width fix | 2026-07-17 | user request (width snapped to longest word) | Docs REFRESH if a rotating-text page exists — not a new item. Wrapper width now animates to the current word: second per-instance keyframe track animates `width` through `--rt-w0..n` vars on the same clock as the word loop; script measures each word after `document.fonts.ready`, sets vars, adds `.rt-fit`. Phase-lock is free — custom properties in keyframes resolve live and both animations started together at style time. Words got `justify-self:start` (else grid-stretch breaks measurement). No-JS/reduced-motion = old longest-word behaviour. No longer ZERO client JS — registry description updated. Board 9 has a width-fit example with extreme length variance. | documented 2026-07-17 |
| map | 2026-07-17 | mapcn map.tsx, phase 1 of docs/plans/phased-maps-plan-port.md | Three files: Map + MapMarker + MapControls. FIRST RUNTIME NPM DEP (`dependencies: ["maplibre-gl"]`) — docs must say the CLI installs it, and that the CONSUMER OWNS CONTAINER HEIGHT (h-full in an unsized parent = 0px map; every demo needs an explicit-height wrapper). Composition: map instance on root as `__astroShadMap` + `map:ready` CustomEvent; children find the map via closest() and handle both script orders. mapcn's 5 marker components collapsed into MapMarker slots: default (content, ACCENT-dot fallback — primary is near-monochrome in the neutral theme and vanished against tiles), popup (template → Popup.setDOMContent, replaces createPortal; toggling is OURS not marker.setPopup — click w/ stopPropagation (load-bearing vs closeOnClick) + Enter/Space + tabindex), tooltip, label. Callback props → CustomEvents on the map root (`map:marker-click`, `map:locate`, `map:moveend` w/ viewport…) — an `id` prop distinguishes markers. Theme: prop > closest `[data-theme]` (makes /dev's scoped toggle work — mapcn only checked documentElement) > doc class > DARK (token default; mapcn's system-preference fallback dropped — absent data-theme MEANS dark, and the watcher is one subtree observer because /dev deletes the attribute in dark mode so closest() at init finds nothing); swap uses setStyle diff:false so `map:style-load` fires for phase-2+ layer children to re-add on (hook exists, nothing uses it yet). Dropped from mapcn: controlled viewport, useMap, generics, `animate-in` popup classes (tw-animate-css not in this repo). is:global CSS neutralises `.maplibregl-popup-content` white chrome + hides the tip. Literals kept w/ comments: pin `border-white`, compass red fills. hover:bg-accent → hover:bg-muted per accent decision. Repo needs `npm i maplibre-gl` before /dev runs. PHASE 2 ADDITION (same day): `MapPopup.astro` joined this item — standalone popup at lng/lat, slot content in the same token card chrome; DIVERGENCE: closeOnClick defaults FALSE (MapLibre default true — a static popup that closes on a stray click can never reopen; opt back in via prop); `map:popup-close` event w/ `id`. Phases 3-4 (geojson, arc/cluster) mapped in the plan doc. | documented 2026-07-17 |
| map-route | 2026-07-17 | mapcn map.tsx, phase 2 of docs/plans/phased-maps-plan-port.md | Single file, child of Map, renders only a hidden config div. GeoJSON line source+layer from a `coordinates` prop; color/width/opacity/dashArray/beforeId; interactive (default true) = pointer cursor + `map:route-click`/`-mouseenter`/`-mouseleave` CustomEvents w/ `id`. FIRST LAYER CHILD: re-adds source+layer on the map root's `map:style-load` event (theme swap wipes style layers — docs MUST show/mention this survives the toggle; the pattern is the template for phase-3/4 layer items). Layer-scoped map.on listeners registered once — safe before layer exists and survive re-adds. Paint colors are canvas values, can't read tokens — default stays mapcn's literal #4285F4, consumers pass a CSS color to brand it (documented in header). Depends on @astro-shad/map. Demo: board 14, solid + dashed green routes between two labeled markers + MapPopup annotation. | documented 2026-07-17 |
| map-geojson | 2026-07-17 | mapcn map.tsx, phase 3 of docs/plans/phased-maps-plan-port.md | Single file, child of Map, hidden config div only. GeoJSON (inline or URL) → fill + outline layers; `fillPaint`/`linePaint` merge over THEME-AWARE monochrome defaults (light #d4d4d4/#ffffff, dark #404040/#171717 — literal hex, canvas paint can't read tokens; re-resolved from the map root's `data-map-theme` stamp on every re-add, which Map.astro now writes exactly for this) or `false` omits a layer. `mergeHoverPaint` ported verbatim: `fillHoverPaint` values become `case` expressions on hover feature-state — REQUIRES `promoteId` naming a unique feature property. `interactive` (default false, needs the fill layer) = pointer cursor + `map:geojson-click`/`map:geojson-hover` CustomEvents (hover fires feature:null on leave; only on feature CHANGE, mapcn parity). Same style-swap re-add pattern as map-route. Generics flattened to plain JSON. Docs pairing: `<Map blank>` for choropleths (board 14 has districts-on-blank w/ hover accent) + a translucent delivery-zone over the street basemap. Depends on @astro-shad/map. | documented 2026-07-17 |
| pulse-grid | 2026-07-17 | react-bits CursorGrid (user-supplied source) | CursorGrid minus react AND minus the cursor (user's call: hover-follow = unnecessary complexity). Pointer listeners replaced by an autonomous driver: jittered setTimeout averaging `speed` bursts/s, each burst = random point + random color, `pulseChance` fraction become the source's expanding click-ring (`pulseSpeed` px/s). Core engine ported near-verbatim (falloff curves, holdTime→fadeDuration cell lifecycle, radial-gradient stroke, dpr clamp 2, wake/idle rAF that self-suspends when nothing is lit). NEW vs source: `colors` array (cells remember which burst's color lit them, Uint8 index per cell; lattice uses colors[0]), IntersectionObserver gates the burst timer off-screen, prefers-reduced-motion = static lattice only. Colors are LITERAL HEX (canvas paint can't read tokens — same rule as map items); default #60a5fa mirrors dark --accent. Root is `pointer-events-none absolute inset-0` — built to drop into a `background` slot; needs a positioned ancestor WITH HEIGHT (docs must say so, like map's container-height warning). Props all on one JSON data-attribute. Config is init-time only (no live prop reactivity — that was react's job). | documented 2026-07-17 |
| cta-02 | 2026-07-17 | cta-01 + pulse-grid | First section whose background-slot fallback is an ANIMATION item, not an image — the §7.8 "effects drop into the slot" model made real. Structure = cta-01 verbatim (band on bg-card, in-band background slot); fallback is `<PulseGrid colors={[blue, violet]} cellSize=56 speed=1.8 gridOpacity=.05 fillOpacity=.1 maxOpacity=.9 />`, no scrim needed (animation sits behind card-contrast text fine — worth an eyeball in light theme). Docs: cross-link cta-01 (image band) vs cta-02 (animated band); customise = tune PulseGrid props in place or fill the slot. Deps: utils, button, pulse-grid — single add pulls the animation too. | documented 2026-07-17 |
| pixel-transition | 2026-07-17 | react-bits PixelTransition (user-supplied source) | Two stacked layers (`first`/`second` NAMED SLOTS, replacing the firstContent/secondContent element props) swap under a random pixel flash on hover (pointer), tap (touch — same detection as source), or focus/blur; tabindex 0. gsap deleted: the random stagger is per-pixel `--px-in`/`--px-out` random fractions BAKED AT BUILD TIME driving two zero-length `steps(1)` animations (px-show fills forwards, px-hide starts a --px-dur later and wins — later-animation-in-list precedence is the mechanism); gsap.delayedCall → visibility transition delayed by --px-dur on the second layer, both directions. Retrigger = `.px-play` class drop + forced reflow + re-add (≙ killTweensOf). `pixelColor` is any LIVE CSS color incl. tokens (DOM divs, not canvas — contrast with pulse-grid's hex rule; default currentColor). Divergences: card chrome token-styled (bg-card/border-border/rounded-xl, NO fixed 300px width — size via class), `aspectRatio` is CSS aspect-ratio (default 1/1 ≙ source's 100% padding; class-set heights override it), reduced-motion = instant swap no flash, no-JS = static first layer. Shown second layer is pointer-events-none (source behaviour — reveal is decorative, card is the surface): interactive second-content is NOT supported, worth stating in docs. | documented 2026-07-17 |
| gsap-hero-01 | 2026-07-19 | docs/plans/motion-system.md Phase 2 (grilled + locked 2026-07-19) | FIRST TIER-2/GSAP ITEM (`dependencies: ["gsap@^3.13.0"]`, caret floor not exact pin — GSAP 3 has no breaking major in years, 3.13 is when every plugin incl. ScrollTrigger went free). Structure mirrors hero-01 (ships populated, same content-block-comment convention, same badge/heading/paragraph/buttons shape) but the `background` slot wrapper is a genuine ScrollTrigger scrub target, not a static layer: `[data-gsap-hero-bg]` oversized `inset-x-0 -top-[15%] -bottom-[15%]` and tweened `yPercent -10→10` linked to scroll via `gsap.fromTo(...scrollTrigger:{scrub:true})` — this is real GSAP territory (continuous scroll-linked, not one-shot), not something @astro-shad/motion (WAAPI) could do, so it's not undercutting the tier split. PORTING PHILOSOPHY DIVERGENCE FROM TIER 1 (deliberate, per the grill): code is stock/near-copy-pasted ScrollTrigger boilerplate, NOT funneled through motion.ts's preset vocabulary or a custom abstraction — future GSAP items should read the same way, so found GSAP templates can be pasted in with minimal translation. Architecture: bespoke per-component `<script>`, no shared tier-2 lib, mirrors Map.astro precedent (`gsap.registerPlugin` is idempotent, safe across multiple instances). `resolveScroller()` walks up for the nearest `overflow-y:auto\|scroll` ancestor before falling back to window — needed because /dev's board preview area is itself a scroll container, same ancestor-walk shape as Map.astro's `resolveTheme()`. `prefers-reduced-motion` guard skips ScrollTrigger creation entirely (early return, no reduced-motion fallback animation — matches how little tier-1 does when reduced-motion is on for triggers with no meaningful static equivalent). KNOWN GAP, stated honestly in the header comment: no `.kill()` cleanup on ScrollTrigger instances — acceptable today because this repo has no Astro view-transitions/`ClientRouter` anywhere (plain MPA nav tears the page down between routes), but flagged as needing an `astro:before-swap` pass if/when this repo or a consumer adopts view-transitions. Docs MUST carry the standard "installs an npm dependency" callout + sidebar badge (docs/plans/motion-system.md Phase 2 says this applies retroactively to `map` too — a docs-session follow-up, not done as part of this row). `package.json` gained `gsap@^3.13.0` in `dependencies` so `/dev` runs locally — same pattern as `map`'s `maplibre-gl` addition. Naming: `gsap-<family>-NN` is now the standing convention for all tier-2 items (not `parallax-hero-01` as the plan first drafted) — file `GsapHero-01.astro`, item `gsap-hero-01`. Pass bar before a second GSAP item ships (docs/plans/motion-system.md Phase 2): (1) registry install path tested end-to-end via `spike-consumer`, not yet done — next step; (2) ScrollTrigger cleanup on nav — see known gap above; (3) code reads as stock GSAP — met. `registry:validate` passed (44 items). | built |
